# Diabetes-detector-test
```
It is a test web application whose function is to detect diabetes, it uses Streamlit, pandas, and the sckit-learn library. The logic of the web application is through Machine Learning models to train a model with the dataset already created and based on new information given by the user to detect whether or not he has positive diabetes.
```
